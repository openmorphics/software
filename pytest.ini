[pytest]
addopts = --import-mode=importlib -ra
markers =
    e2e: End-to-end integration tests across SAL/CLI/backends.
    conformance: Trace equivalence and determinism contract tests.
    native: Native extension parity/behavior tests.
    perf: Performance and benchmark gate tests.
    stub: Tests that validate known stub behavior.
